// Fallback nativo requerido por expo-router. El panel es solo web: en nativo el
// contenido lo cubre app/admin/_layout.tsx (aviso "solo web").
export default function AdminParkingsNative() {
  return null;
}
